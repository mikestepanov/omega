#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Pre-push hook for bot tests
echo "🧪 Running bot tests before push..."

cd bots

# Run quick smoke tests
npm test || {
    echo "❌ Tests failed!"
    exit 1
}

echo "✅ Pre-push tests passed!"